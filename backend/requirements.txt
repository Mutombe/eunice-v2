# Backend dependencies — pinned for reproducible deploys.
# Render reads this file at build time.
Django==5.2.14
djangorestframework==3.17.1
django-cors-headers==4.9.0
pillow==12.2.0
python-dotenv==1.2.2

# Database (Postgres via Neon — psycopg v3 for channel-binding support)
psycopg[binary]==3.3.4
dj-database-url==3.1.2

# Media storage (DigitalOcean Spaces, S3-compatible)
django-storages==1.14.6
boto3==1.43.14

# Production server + static files
gunicorn==26.0.0
whitenoise==6.12.0
